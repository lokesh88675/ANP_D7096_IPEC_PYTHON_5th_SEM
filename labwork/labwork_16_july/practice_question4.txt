# Employee Salary Calculator 
# making function 

 def calculate_hra(basic):
    return salary*.2
def calculate_da(basic)
 return salary*.10

def calculate_tax(gross_salary) 
   return gross_salary*.08



calculate_net_salary(basic) 
    x = salary-(calculate_hra(basic)+calculate_da(basic)+calculate_tax(gross_salary))
  return x 
salary = int (input("enter your salary ")
gross_salary = salary-(calculate_da(basic)+calculate_hra(basic))


print ("the basic salary is ", salary )
print ("the HRA is ", calculate_hra(basic))
print ("the DA  is ", calculate_da(basic) )
print ("the GROSS_SALARY  is ", gross_salary)
print ("the TAX is ", calculate_tax(gross_salary))
print("the net salary is ",calculate_net_salary(basic) )



